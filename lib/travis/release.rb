# frozen_string_literal: true

require 'travis/release/version'
require 'travis/release/task'

module Travis
  # Adds a `:release` task for releasing gems to RubyGems, via Travis CI.
  module Release
  end
end
